apiVersion: v1
kind: Service
metadata:
  name: claudekit-docs
  labels:
    app: claudekit-docs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: claudekit-docs
